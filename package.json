{
  "name": "flucht",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha --compilers js:babel-core/register test/*",
    "make": "rollup -c -o ./bin/rolledup.js",
    "watch": "watch rollup -c -o ./bin/rolledup.js"
  },
  "devDependencies": {
    "babel-core": "^6.14.0",
    "babel-plugin-external-helpers": "^6.8.0",
    "babel-preset-es2015": "^6.14.0",
    "chai": "3.5.0",
    "mocha": "^3.0.2"
  }
}
